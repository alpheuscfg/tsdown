packages:
    - ./package
    - ./tests/*
    - ./examples/*

catalogs:
    dev:
        "@types/node": ^25
        # Minimum supported version
        "tsdown": 0.21.0
    peer:
        # Supported versions
        "tsdown": ~0.21.0
    test:
        "vitest": ^4

minimumReleaseAge: 1440

minimumReleaseAgeExclude:
    - "@apst/biome"
    - "@apst/tsconfig"
    - "@rolldown/*"
    - "rolldown"
    - "tsdown"
